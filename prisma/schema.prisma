generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // لاحظ إننا شيلنا سطر الـ url من هنا تماماً حسب تحديث Prisma 7
}

// جدول تجريبي
model User {
  id    String @id @default(cuid())
  email String @unique
  name  String?
}